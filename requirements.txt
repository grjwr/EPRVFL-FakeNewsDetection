# Core dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Transformer embeddings
transformers>=4.40
torch>=2.0

# Deep learning baselines (optional — only needed for --run_dl_baselines)
tensorflow>=2.13

# Visualisation (optional — for plotting results)
matplotlib>=3.7
seaborn>=0.13
